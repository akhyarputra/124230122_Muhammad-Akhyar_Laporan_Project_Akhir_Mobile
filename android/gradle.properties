org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true

# If your system Java (JAVA_HOME) is still Java 8, you can point Gradle to a Java 11+ JDK
# by uncommenting and setting the line below to your JDK 11/17 installation path.
# Example (Windows):
# org.gradle.java.home=C:\Program Files\Java\jdk-17
# After changing this value restart your terminal/IDE and run `.
# gradlew.bat --stop` then try the build again.

# Set Gradle's JDK for this project. Updated to the JDK path you set with setx.
## Ensure path uses forward slashes or escaped backslashes. Using forward slashes avoids escaping issues.
org.gradle.java.home=D:/Eclipse Adoptium/jdk-17.0.17.10-hotspot
